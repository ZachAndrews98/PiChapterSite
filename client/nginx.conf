

# server {
#   listen 80;
#   server_name client;

#   location / {
#     root /usr/share/nginx/html;
#     try_files $uri /index.html;
#   }

#   location /api {
#     proxy_pass         http://api:8080;
#   }
# }

server {
  listen 80;
  server_name client;

  location / {
    root /usr/share/nginx/html;
    try_files $uri /index.html;
  }

  location /api {
    proxy_pass         https://proxy.zachandrews.com/pi-api;
  }
}

/** DataSonnet
version=2.0
output application/json
input payload application/json
*/

{
    "firstName": ds.select(payload, "firstName"),
    "lastName": ds.select(payload, "lastName"),
    "email": ds.select(payload, "email"),
    "phone": ds.select(payload, "phone"),
    "gradYear": ds.select(payload, "gradYear"),
    "major": ds.select(payload, "major"),
    "minor": ds.select(payload, "minor"),
    "status": ds.select(payload, "status"),
    "roleId": ds.select(payload, "roleId")
}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pichapter-ingress
spec:
  rules:
  - host: test.alleghenyfiji.org
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: client-svc
            port: 
              number: 80
